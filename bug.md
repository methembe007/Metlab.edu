# Current situations


- the UI is not working properly
- Toggle is  has no functionality i assume that the  the code is  missing in the static/css/mobile-optimizxation


- video_call app also has a bug in views.py. The AI ran into a error and aborted.


# Authentication systems 

1. when a user registers a email api is triggered, however the the code does send the the  the verification link that must be sent doesn't work yet 

2. In order  for the parents to link their child they require  that the child go to their profile setting, howerver  there's no profile setting 


3. Unfortunately the videos  chat systems  is not working properly> The UI is cannot work , but dont have any functionality  to them.  Need to have ai run some tests with effective immediately 